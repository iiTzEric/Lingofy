import { jsx } from 'react/jsx-runtime';
import { OwnCapability } from '@stream-io/video-client';
import { useCallStateHooks } from '@stream-io/video-react-bindings';
import { HostStateRouter } from './host/HostStateRouter.js';
import { ViewerStateRouter } from './viewer/ViewerStateRouter.js';

const LivestreamUI = () => {
    const { useHasPermissions } = useCallStateHooks();
    const isHost = useHasPermissions(OwnCapability.JOIN_BACKSTAGE);
    if (isHost) {
        return jsx(HostStateRouter, {});
    }
    return jsx(ViewerStateRouter, {});
};

export { LivestreamUI };
//# sourceMappingURL=LivestreamUI.js.map
