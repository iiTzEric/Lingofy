import { useEffect } from 'react';
import { useCall } from '@stream-io/video-react-bindings';

const useTrackElementVisibility = ({ trackedElement, sessionId, trackType, }) => {
    const call = useCall();
    useEffect(() => {
        if (!trackedElement || !call || trackType === 'none')
            return;
        return call.trackElementVisibility(trackedElement, sessionId, trackType);
    }, [trackedElement, call, sessionId, trackType]);
};

export { useTrackElementVisibility };
//# sourceMappingURL=useTrackElementVisibility.js.map
