import { VideoTrackType } from '@stream-io/video-client';
export declare const useTrackElementVisibility: <T extends HTMLElement>({ trackedElement, sessionId, trackType, }: {
    trackedElement: T | null;
    sessionId: string;
    trackType: VideoTrackType | "none";
}) => void;
