import { jsx } from 'react/jsx-runtime';
import { CompositeButton } from '../Button/CompositeButton.js';
import '../Button/IconButton.js';
import { useI18n } from '@stream-io/video-react-bindings';
import { Icon } from '../Icon/Icon.js';
import { useState } from 'react';
import '@stream-io/video-client';
import '../DeviceSettings/DeviceSettings.js';
import 'clsx';
import '@floating-ui/react';
import '../DropdownSelect/DropdownSelect.js';
import '../Menu/MenuToggle.js';
import { DeviceSelectorAudioOutput } from '../DeviceSettings/DeviceSelectorAudio.js';
import { WithTooltip } from '../Tooltip/WithTooltip.js';

const ToggleAudioOutputButton = (props) => {
    const { t } = useI18n();
    const { caption, Menu = DeviceSelectorAudioOutput, menuPlacement = 'top', onMenuToggle, } = props;
    const [tooltipDisabled, setTooltipDisabled] = useState(false);
    return (jsx(WithTooltip, { title: caption || t('Speakers'), tooltipDisabled: tooltipDisabled, children: jsx(CompositeButton, { Menu: Menu, menuPlacement: menuPlacement, caption: caption, "data-testid": "audio-output-button", onMenuToggle: (shown) => {
                setTooltipDisabled(shown);
                onMenuToggle?.(shown);
            }, children: jsx(Icon, { icon: "speaker" }) }) }));
};

export { ToggleAudioOutputButton };
//# sourceMappingURL=ToggleAudioOutputButton.js.map
