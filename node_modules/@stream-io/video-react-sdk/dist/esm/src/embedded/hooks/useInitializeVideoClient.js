import { useState, useRef, useEffect } from 'react';
import { StreamVideoClient } from '@stream-io/video-client';

/**
 * Hook that creates a StreamVideoClient and connects the user.
 * Disconnects and cleans up on unmount or when props change.
 *
 */
const useInitializeVideoClient = ({ apiKey, user, token, tokenProvider, logLevel, handleError, }) => {
    const [client, setClient] = useState();
    const tokenProviderRef = useRef(tokenProvider);
    tokenProviderRef.current = tokenProvider;
    useEffect(() => {
        if (!apiKey)
            return;
        const options = logLevel ? { logLevel } : undefined;
        let _client;
        try {
            if (user?.type === 'guest') {
                const streamUser = {
                    id: user.id,
                    type: 'guest',
                    name: user.name,
                    image: user.image,
                };
                _client = new StreamVideoClient({
                    apiKey,
                    user: streamUser,
                    options,
                });
            }
            else if (user?.type === 'anonymous') {
                const streamUser = {
                    id: '!anon',
                    type: 'anonymous',
                    name: user.name,
                    image: user.image,
                };
                _client = new StreamVideoClient({
                    apiKey,
                    user: streamUser,
                    token,
                    tokenProvider: tokenProviderRef.current,
                    options,
                });
            }
            else {
                const streamUser = {
                    id: user.id,
                    name: user.name,
                    image: user.image,
                };
                const currentTokenProvider = tokenProviderRef.current;
                _client = new StreamVideoClient({
                    apiKey,
                    user: streamUser,
                    ...(token
                        ? {
                            token,
                            ...(currentTokenProvider
                                ? { tokenProvider: currentTokenProvider }
                                : {}),
                        }
                        : { tokenProvider: currentTokenProvider }),
                    options,
                });
            }
            setClient(_client);
        }
        catch (err) {
            handleError(err);
        }
        return () => {
            _client
                ?.disconnectUser()
                .catch((err) => console.error('Failed to disconnect user:', err));
            setClient(undefined);
        };
    }, [
        apiKey,
        user.id,
        user.type,
        user.name,
        user.image,
        token,
        logLevel,
        handleError,
    ]);
    return client;
};

export { useInitializeVideoClient };
//# sourceMappingURL=useInitializeVideoClient.js.map
