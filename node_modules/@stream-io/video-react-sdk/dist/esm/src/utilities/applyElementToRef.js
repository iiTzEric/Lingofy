const applyElementToRef = (ref, element) => {
    if (!ref)
        return;
    if (typeof ref === 'function')
        return ref(element);
    ref.current = element;
};

export { applyElementToRef };
//# sourceMappingURL=applyElementToRef.js.map
