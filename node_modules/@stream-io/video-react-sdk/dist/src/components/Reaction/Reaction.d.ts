import { StreamVideoParticipant } from '@stream-io/video-client';
export type ReactionProps = {
    participant: StreamVideoParticipant;
    hideAfterTimeoutInMs?: number;
    emojiReactionMap?: Record<string, string>;
};
export declare const defaultEmojiReactionMap: Record<string, string>;
export declare const Reaction: ({ participant: { reaction, sessionId }, hideAfterTimeoutInMs, emojiReactionMap, }: ReactionProps) => import("react").JSX.Element | null;
