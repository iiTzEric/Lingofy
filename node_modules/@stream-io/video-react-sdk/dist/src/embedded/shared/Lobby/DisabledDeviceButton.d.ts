interface DisabledDeviceButtonProps {
    icon: string;
    label: string;
}
export declare const DisabledDeviceButton: ({ icon, label, }: DisabledDeviceButtonProps) => import("react").JSX.Element;
export {};
