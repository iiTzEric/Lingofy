import { jsx, jsxs, Fragment } from 'react/jsx-runtime';
import { CallingState } from '@stream-io/video-client';
import { AcceptCallButton } from '../CallControls/AcceptCallButton.js';
import { useCall, useCallStateHooks } from '@stream-io/video-react-bindings';
import 'react';
import 'clsx';
import '@floating-ui/react';
import '../CallControls/RecordCallButton.js';
import '../CallControls/ReactionsButton.js';
import '../Button/CompositeButton.js';
import '../Button/IconButton.js';
import '../DeviceSettings/DeviceSettings.js';
import '../DropdownSelect/DropdownSelect.js';
import '../Menu/MenuToggle.js';
import { CancelCallButton } from '../CallControls/CancelCallButton.js';
import '../CallControls/CallStatsButton.js';

const RingingCallControls = () => {
    const call = useCall();
    const { useCallCallingState } = useCallStateHooks();
    const callCallingState = useCallCallingState();
    if (!call)
        return null;
    const buttonsDisabled = callCallingState !== CallingState.RINGING;
    return (jsx("div", { className: "str-video__pending-call-controls", children: call.isCreatedByMe ? (jsx(CancelCallButton, { disabled: buttonsDisabled })) : (jsxs(Fragment, { children: [jsx(AcceptCallButton, { disabled: buttonsDisabled }), jsx(CancelCallButton, { onClick: () => {
                        const reason = call.isCreatedByMe ? 'cancel' : 'decline';
                        call.leave({ reject: true, reason }).catch((err) => {
                            console.error(`Failed to reject`, err);
                        });
                    }, disabled: buttonsDisabled })] })) }));
};

export { RingingCallControls };
//# sourceMappingURL=RingingCallControls.js.map
